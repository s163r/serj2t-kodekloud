---
- name: Prepare application nodes
  hosts: app
  become: true

  tasks:
    - name: Install the latest version of iptables and croni
      ansible.builtin.package:
        name:
          - iptables
        state: latest